<div class="content">
  <%= render 'form' %>
  <div class="container">
    <% if user_signed_in? && @wiki.private %>
        <%= render partial: 'collaborators/form', locals: { wiki: @wiki } %>
        <%= render partial: 'collaborators/show' %>
    <% end %>
  </div>
</div>

